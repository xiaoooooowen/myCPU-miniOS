# artifacts

此目录存放仓库生成的产物，默认不进入版本控制。

| 目录 | 用途 |
|------|------|
| `reports/` | 项目报告输出（PDF、LaTeX 等） |
| `logs/` | 运行日志、测试日志 |
| `screenshots/` | 运行截图、演示输出 |

如需将某个生成物纳入版本控制（如终版报告），请手动 `git add -f` 添加。
